# 📚 LicenseCore++ - Полное руководство по сборке

Это подробная инструкция для сборки библиотеки LicenseCore++ в продакшен. Руководство написано для людей без опыта программирования.

## 🎯 Что такое LicenseCore++?

LicenseCore++ - это библиотека для проверки лицензий в ваших программах. Она умеет:
- Определять характеристики компьютера
- Проверять валидность лицензий  
- Защищать ваше программное обеспечение от пиратства

## 📦 Три варианта библиотеки

У нас есть **три разные версии** библиотеки для разных задач:

| Версия | Назначение | Когда использовать |
|--------|------------|-------------------|
| **Основная** | Разработка и тестирование | Когда пишете код, тестируете |
| **Embedded** | Простая интеграция | Для простых проектов, C-программы |
| **Obfuscated** | Максимальная защита | Коммерческие продукты, защита от взлома |

---

## 🔧 ПОДГОТОВКА СИСТЕМЫ

### Что нужно установить ПЕРЕД началом:

#### macOS (Mac):
```bash
# 1. Установите Xcode Command Line Tools
xcode-select --install

# 2. Установите Homebrew (менеджер пакетов)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 3. Установите необходимые пакеты
brew install cmake openssl git make
```

#### Linux (Ubuntu/Debian):
```bash
# Обновите систему
sudo apt update && sudo apt upgrade -y

# Установите необходимые пакеты
sudo apt install -y build-essential cmake git libssl-dev pkg-config
```

#### Windows:
```bash
# Установите MinGW-w64 или Visual Studio 2019+
# Скачайте и установите CMake с сайта cmake.org
# Установите Git с сайта git-scm.com
```

### Скачивание исходного кода:
```bash
# Клонируйте репозиторий
git clone <ваш-репозиторий-url> LicenseCore
cd LicenseCore

# Проверьте, что все файлы на месте
ls -la
# Должны увидеть папки: src, include, gtests, embedded, obfuscated
```

---

## 🏗️ СБОРКА 1: ОСНОВНАЯ БИБЛИОТЕКА

**Для чего:** Разработка, тестирование, отладка

### Пошаговая инструкция:

#### Шаг 1: Автоматическая сборка
```bash
# Перейдите в корневую папку проекта
cd LicenseCore

# Запустите автоматическую сборку
./build.sh
```

**Что происходит:**
- Создаётся папка `build/`
- Компилируется библиотека
- Запускаются 56 автоматических тестов
- Создаются примеры программ

#### Шаг 2: Проверка результата
```bash
# Если всё прошло успешно, увидите:
# "100% tests passed, 0 tests failed out of 56"
# "✅ Library built: liblicensecore.a"
```

#### Шаг 3: Ручная сборка (если автоматическая не работает)
```bash
# Создайте папку для сборки
mkdir -p build
cd build

# Настройте проект
cmake .. -DCMAKE_BUILD_TYPE=Release

# Соберите библиотеку
make -j4

# Запустите тесты
make test
```

### 📂 Что получится:
```
build/
├── liblicensecore.a          ← Основная библиотека
├── examples/
│   ├── simple_example        ← Простой пример
│   ├── license_generator     ← Генератор лицензий
│   └── hwid_tool            ← Утилита для получения ID железа
└── gtests/                  ← Тестовые программы
```

### 🧪 Как проверить что всё работает:
```bash
# Запустите простой пример
cd build
./examples/simple_example

# Должны увидеть что-то вроде:
# "Hardware ID: A1B2C3D4E5F6..."
# "License validation: SUCCESS"
```

---

## 🔧 СБОРКА 2: EMBEDDED ВЕРСИЯ

**Для чего:** Простая интеграция в C-проекты, встраиваемые системы

### Пошаговая инструкция:

#### Шаг 1: Переход в папку embedded
```bash
# Из корневой папки проекта
cd embedded

# Посмотрите что доступно
make help
```

#### Шаг 2: Сборка библиотеки
```bash
# Очистите предыдущие сборки
make clean

# Соберите библиотеку
make all

# Должны увидеть:
# "✅ Library created: liblicense_core.a"
```

#### Шаг 3: Тестирование
```bash
# Соберите и запустите тест
make test

# Запустите тестовую программу
./test_embedded
```

#### Шаг 4: Создание релизной версии
```bash
# Создайте обфусцированную версию
make obfuscated

# Установите в папку release
make install

# Создайте пакет для распространения
make package
```

### 📂 Что получится:
```
embedded/
├── liblicense_core.a         ← Библиотека для C-проектов
├── test_embedded            ← Тестовая программа
└── ../release/
    ├── lib/liblicense_core.a
    └── include/license_core_stub.h
```

### 🧪 Как проверить:
```bash
# Запустите тест
./test_embedded

# Должны увидеть:
# "=== LicenseCore++ Embedded Test ==="
# "🖥️ Hardware ID: ..."
# "🔐 License validation: ✅ VALID"
```

### 💻 Как использовать в своём C-проекте:
```c
// Включите заголовочный файл
#include "license_core_stub.h"

int main() {
    // Получите ID железа
    const char* hwid = lc_get_hwid();
    printf("Hardware ID: %s\n", hwid);
    
    // Проверьте лицензию
    const char* license_json = "..."; // Ваша лицензия
    if (lc_validate_license(license_json)) {
        printf("Лицензия валидна!\n");
        
        // Проверьте наличие функции
        if (lc_has_feature("premium")) {
            printf("Премиум функции доступны!\n");
        }
    } else {
        printf("Лицензия невалидна!\n");
    }
    
    return 0;
}
```

```bash
# Компиляция вашего проекта:
gcc -o myprogram myprogram.c liblicense_core.a -framework IOKit -framework CoreFoundation
```

---

## 🔒 СБОРКА 3: OBFUSCATED ВЕРСИЯ (ENTERPRISE)

**Для чего:** Коммерческие проекты, максимальная защита от взлома

### ⚠️ ВАЖНО: Требования безопасности
- Собирайте только на доверенной машине
- Не передавайте исходники третьим лицам
- Используйте только для финальных релизов

### Пошаговая инструкция:

#### Шаг 1: Переход в папку obfuscated
```bash
# Из корневой папки проекта
cd obfuscated

# Посмотрите доступные опции
make help
```

#### Шаг 2: Максимальная защита
```bash
# Очистите предыдущие сборки
make clean

# Создайте версию с максимальной обфускацией
make obfuscated

# Должны увидеть:
# "🔐 Applying maximum obfuscation..."
# "✅ Obfuscated library ready: liblicense_core.a"
```

#### Шаг 3: Анализ безопасности
```bash
# Проанализируйте уровень защиты
make analysis

# Посмотрите детальную информацию
make info
```

#### Шаг 4: Тестирование
```bash
# Соберите и запустите security-тест
make test

# Должны увидеть отчёт о безопасности
```

#### Шаг 5: Создание финального пакета
```bash
# Создайте enterprise-пакет для клиента
make package
```

### 📂 Что получится:
```
obfuscated/
├── liblicense_core.a                    ← Защищённая библиотека
├── symbols.map                          ← Карта переименованных символов
├── test_obfuscated                     ← Security-тест
└── ../release_obfuscated/
    ├── lib/liblicense_core.a
    ├── include/license_core_stub.h
    ├── docs/symbols.map
    └── licensecore-obfuscated-YYYYMMDD.tar.gz
```

### 🛡️ Уровни защиты:

#### Базовая обфускация (`make all`):
- Скрытие символов
- Переименование функций
- Удаление отладочной информации

#### Максимальная защита (`make obfuscated`):
- Всё из базовой +
- Сжатие UPX (если доступно)
- Удаление служебных секций
- Оптимизация по размеру

#### Anti-debug версия (`make anti-debug`):
- Всё из максимальной +
- Защита от отладчиков
- Защита от анализа во время выполнения

### 🧪 Как проверить защиту:
```bash
# Запустите security-тест
./test_obfuscated

# Должны увидеть:
# "🔒 Symbol obfuscation: ✅ Applied"
# "🛡️ Anti-debug: ✅ Active"
# "🔐 Embedded license: ✅ Encrypted"
```

### 💻 Интеграция в клиентский проект:
```c
// Точно такой же API как у embedded версии
#include "license_core_stub.h"

int main() {
    // API не изменился, но внутри всё обфусцировано
    if (lc_validate_embedded()) {  // Встроенная лицензия
        printf("Enterprise лицензия валидна!\n");
    }
    
    return 0;
}
```

**Разница от embedded:** Символы в библиотеке переименованы:
- `lc_validate_license` → `_lX9aV`
- `lc_has_feature` → `_f7vKe`  
- `lc_get_hwid` → `_hW3iD`

---

## 🚀 ПРОИЗВОДСТВЕННОЕ РАЗВЁРТЫВАНИЕ

### Для разработки:
```bash
# Используйте основную библиотеку
./build.sh
# Получите: build/liblicensecore.a
```

### Для клиентов (простые проекты):
```bash
cd embedded
make package
# Отправьте клиенту: release/licensecore-embedded-YYYYMMDD.tar.gz
```

### Для коммерческих продуктов:
```bash
cd obfuscated  
make obfuscated
make package
# Отправьте клиенту: release_obfuscated/licensecore-obfuscated-YYYYMMDD.tar.gz
```

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ

### Проблема: "cmake: command not found"
```bash
# macOS:
brew install cmake

# Linux:
sudo apt install cmake

# Windows: скачайте с cmake.org
```

### Проблема: "OpenSSL not found"
```bash
# macOS:
brew install openssl
export PKG_CONFIG_PATH="/opt/homebrew/lib/pkgconfig"

# Linux:
sudo apt install libssl-dev
```

### Проблема: "Tests failed"
```bash
# Запустите тесты с подробным выводом
cd build
ctest --output-on-failure --verbose
```

### Проблема: "Permission denied"
```bash
# Дайте права на выполнение скриптам
chmod +x build.sh
chmod +x embedded/build.sh
chmod +x obfuscated/build_obfuscated.sh
```

### Проблема: "No such file or directory"
```bash
# Убедитесь что находитесь в правильной папке
pwd
ls -la
# Должны увидеть файлы: CMakeLists.txt, build.sh, src/, include/
```

---

## 📋 ЧЕКЛИСТ ПЕРЕД РЕЛИЗОМ

### ✅ Основная библиотека:
- [ ] `./build.sh` выполнился без ошибок
- [ ] Все 56 тестов прошли (100% success)
- [ ] `./examples/simple_example` работает
- [ ] `build/liblicensecore.a` создался

### ✅ Embedded версия:
- [ ] `make clean && make all` прошёл без ошибок
- [ ] `make test` показал "✅ VALID"
- [ ] `./test_embedded` работает корректно
- [ ] `make package` создал tar.gz файл

### ✅ Obfuscated версия:
- [ ] `make obfuscated` прошёл без ошибок
- [ ] `make analysis` показал скрытые символы
- [ ] `make test` показал активную защиту
- [ ] `make package` создал enterprise пакет

---

## 📞 ПОЛУЧЕНИЕ ПОМОЩИ

### Проверка версий:
```bash
# Проверьте версии установленных инструментов
cmake --version      # Должно быть 3.16+
gcc --version        # Или clang --version  
make --version
git --version
```

### Логи сборки:
```bash
# Сохраните логи сборки для анализа
./build.sh 2>&1 | tee build.log
make all 2>&1 | tee embedded_build.log
make obfuscated 2>&1 | tee obfuscated_build.log
```

### Информация о системе:
```bash
# Соберите информацию о системе
uname -a                    # Операционная система
lscpu                      # Linux: информация о CPU
sysctl -n hw.model         # macOS: модель CPU
echo $PATH                 # Переменная PATH
```

---

## 🎉 ЗАКЛЮЧЕНИЕ

Теперь у вас есть три версии библиотеки LicenseCore++:

1. **Основная** - для разработки и тестирования
2. **Embedded** - для простой интеграции в C-проекты  
3. **Obfuscated** - для коммерческих продуктов с максимальной защитой

Каждая версия имеет свои тесты и готова к использованию в продакшене!

**Удачной разработки! 🚀**
