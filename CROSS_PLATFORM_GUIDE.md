# 🔒 LicenseCore++ Cross-Platform Setup Guide

## 🎯 Quick Answer: На Mac собирается только для Mac!

Ваша текущая сборка работает **только на macOS**. Для других платформ нужна кроссплатформенная сборка.

## 🚀 Варианты решения:

### 1. 🏗️ Локальная мультиплатформенная сборка (рекомендуется)

```bash
# Установите зависимости
brew install mingw-w64      # Для Windows
brew install docker         # Для Linux
# Docker Desktop должен быть запущен

# Запустите мультиплатформенную сборку
chmod +x build_multiplatform_complete.sh
./build_multiplatform_complete.sh
```

**Результат:** Библиотеки для macOS (Intel + Apple Silicon), Linux x64, Windows x64

---

### 2. ☁️ GitHub Actions (автоматическая сборка)

```bash
# 1. Загрузите код в GitHub
git add .
git commit -m "Add cross-platform CI/CD"
git push

# 2. Создайте релиз
git tag v1.0.0
git push --tags

# 3. GitHub автоматически соберет для всех платформ
```

**Результат:** Автоматические релизы с библиотеками для всех платформ

---

### 3. 🧪 Тестирование GitHub Actions локально

```bash
# Установите act
brew install act

# Тест локально
chmod +x test_github_actions.sh
./test_github_actions.sh
```

---

## 📦 Что вы получите:

### Платформы:
- ✅ **macOS Intel** (x86_64)
- ✅ **macOS Apple Silicon** (ARM64) 
- ✅ **macOS Universal** (Intel + Apple Silicon)
- ✅ **Linux x64** (через Docker)
- ✅ **Windows x64** (через MinGW)

### Файлы:
```
package/
├── lib/
│   ├── liblicense_core_macos_universal.a    # Универсальная для Mac
│   ├── liblicense_core_linux_x64.a          # Linux
│   └── liblicense_core_windows_x64.a        # Windows
├── include/
│   └── license_core_pure_c.h                # Заголовочный файл
├── examples/
│   ├── basic_usage.c                        # Пример использования
│   └── Makefile                             # Автоопределение платформы
└── README.md                                # Документация
```

---

## 🔧 Использование клиентами:

### macOS:
```bash
gcc app.c -L./lib -llicense_core_macos_universal \
  -framework IOKit -framework CoreFoundation
```

### Linux:
```bash
gcc app.c -L./lib -llicense_core_linux_x64
```

### Windows:
```bash
gcc app.c -L./lib -llicense_core_windows_x64
```

---

## 📊 Сравнение методов:

| Метод | Скорость | Удобство | Автоматизация | Подходит для |
|-------|----------|----------|---------------|--------------|
| Локальная сборка | ⚡ Быстро | 🔧 Средне | ❌ Ручная | Разработка, тестирование |
| GitHub Actions | 🐌 Медленно | ✅ Легко | ✅ Полная | Продакшн, релизы |
| Локальный тест | ⚡ Быстро | 🔧 Сложно | ⚠️ Частичная | Отладка CI/CD |

---

## 🎯 Рекомендации:

### Для разработки:
1. Используйте **локальную сборку** для быстрого тестирования
2. Настройте **GitHub Actions** для автоматических релизов

### Для клиентов:
1. Предоставляйте **универсальную macOS библиотеку**
2. Включайте **примеры и Makefile** с автоопределением платформы
3. Создавайте **релизы через GitHub** с готовыми пакетами

---

## 🚀 Начните прямо сейчас:

```bash
# Быстрый старт - локальная сборка
chmod +x build_multiplatform_complete.sh
./build_multiplatform_complete.sh

# Тест результата
cd local_multiplatform_build/package/examples
make && make test
```

**Готово!** Теперь у вас есть библиотеки для всех основных платформ! 🎉
